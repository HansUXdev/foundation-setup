module.exports = function(grunt) {

  grunt.config('compass', {
    dist:{
      options:{
        // watch: true,
        config: 'config.rb',
        environment: 'development'
      }
    },
  });


};
